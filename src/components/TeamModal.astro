---
---

<div class="modal" id="bio-modal" role="dialog" aria-labelledby="bio-title" aria-modal="true">
  <div class="modal__dialog">
    <div class="modal__head">
      <div>
        <h3 id="bio-title" class="modal__title">Member name</h3>
        <p id="bio-role" class="modal__role">Role</p>
      </div>
      <button class="modal__close" type="button" id="bio-close" aria-label="Close bio dialog">×</button>
    </div>
    <div class="modal__body">
      <img id="bio-img" class="modal__avatar" src="" alt="Profile photo" />
      <p id="bio-desc">Bio text here.</p>
    </div>
  </div>
</div>

